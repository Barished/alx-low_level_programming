#include <stdio.h>
#include "main.h"

/**
 *_putchar - writes the character to standard output
 *@c: The character to print
 *
 *Return: On success 1
 *On error, -1 is returned
 */

int _putchar(char s)
{
	return (write(1, &c, 1);
}
